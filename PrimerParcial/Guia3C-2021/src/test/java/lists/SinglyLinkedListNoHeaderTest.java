package lists;

import org.junit.jupiter.api.Test;

import java.util.Arrays;

import static lists.ListMatchers.assertListsMatch;

class SinglyLinkedListNoHeaderTest {

  @Test
  void singleLinkedListWorks() {
//    LinkedListNoHeader<Integer> list = new SinglyLinkedListNoHeader<>(3, null);
//    list = list.addLast(4).addFirst(2).addLast(5).addFirst(1).remove(4);
//    assertListsMatch(list, Arrays.asList(1, 2, 3, 5));
  }
}